var a = 0;
var b = 0;
var CalcularMultiplicación = 0;

a = parseInt(prompt('Digita un número a'));
b = parseInt(prompt('Digita un número b'));
CalcularMultiplicación = Multiplicación (a,b);

function Multiplicación(nA, nB){
    const result = nA * nB;
    console.log('El resultado de la multiplicación es el siguiente: ' + result);
}